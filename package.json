{
  "name": "subscription-management-system",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix ./server\" \"npm run client --prefix ./client\"",
    "server": "nodemon server.js --prefix ./server",
    "client": "npm start --prefix ./client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.7"
  }
}
